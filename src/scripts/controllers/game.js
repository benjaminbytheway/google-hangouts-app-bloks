'use strict';

define('controllers/game', [
    'app',
    'player',
    'events'
  ], function (app, Player, events) {

    app.controller('GameController', ['$rootScope', function ($rootScope) {
        // TODO;
      }]);

  });